name: Test ARC Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: arc-runner-set

    steps:
      - uses: actions/checkout@v4

      - name: Test runner
        run: |
          echo "Running on ARC"
          hostname
          uname -a
